# 智能车间轻量级 MES 系统 —— 需求规格说明书（SRS）

> **版本**: v1.0  
> **日期**: 2026-06-12  
> **项目**: Smart Workshop Lightweight MES System (Backend)  
> **文档类型**: Software Requirements Specification（基于现有设计文档逆向推导）

---

## 目录

1. [引言](#1-引言)
2. [综合描述](#2-综合描述)
3. [外部接口需求](#3-外部接口需求)
4. [系统特性](#4-系统特性)
5. [非功能需求](#5-非功能需求)
6. [附录](#6-附录)

---

## 1. 引言

### 1.1 编写目的

本文档描述"智能车间轻量级 MES 系统"后端的功能性与非功能性需求，作为开发、测试、验收以及后续维护升级的统一基线。本文档面向以下读者：

- 项目经理与产品负责人：理解系统目标、范围与交付边界。
- 开发与测试工程师：作为接口、业务规则与质量属性的实现依据。
- 运维与部署人员：明确系统的运行环境、外部依赖与部署约束。
- 客户/业务方：确认本系统是否满足车间生产管理的实际诉求。

### 1.2 项目背景

本系统面向中小型制造企业的车间现场管理，解决从**生产计划 → 生产订单 → 工单**的三级生产调度与执行跟踪问题，并提供物料清单（BOM）、工艺流程、工序、工作步骤、设备、产线、班组、技能矩阵等主数据维护，以及基于角色的访问控制（RBAC）。系统采用轻量级单体多模块架构，便于中小型企业部署与维护。

### 1.3 文档范围

本文档覆盖：

- 系统在生产、主数据、权限、安全、审计等领域的功能需求；
- 系统对外提供的 RESTful API 与对内依赖的软件、硬件、通信接口需求；
- 性能、可靠性、安全、可维护性、可扩展性等非功能性指标。

不在本文档范围内：

- 前端 Web/移动端 UI 详细设计；
- 第三方 ERP/WMS 集成的具体协议（仅在外部接口章节做开放性描述）；
- 企业级容灾、多活、跨地域部署等高阶运维方案。

### 1.4 术语与缩略语

详见 [6.1 术语表](#61-术语表) 与 [6.2 缩略语](#62-缩略语)。

### 1.5 参考资料

- `docx/概要设计说明书.md`（主要输入）
- `docx/智联车间轻量化MES系统数据库设计.md`
- `docx/详细设计/01-系统架构详细设计.md` ~ `08-安全认证详细设计.md`
- `docx/三层生产架构/mes三层计划_订单_工单状态与权限设计总结.md`
- `docx/三层生产架构/计划管理/计划管理模块_API接口文档.md`
- `docx/三层生产架构/订单管理/订单管理模块_API接口文档.md`
- `docx/三层生产架构/工单管理/工单管理模块_API接口文档.md`
- `docx/RBAC权限/RBAC权限管理系统接口设计.md`
- `docx/静态信息维护/智联车间轻量化MES系统接口文档.md`

### 1.6 文档约定

- 关键词 **SHALL / MUST / 必须**：表示强制性需求。
- 关键词 **SHOULD / 应当**：表示建议性需求。
- 关键词 **MAY / 可以**：表示可选需求。
- 所有 RESTful URL 使用相对路径，省略 Host 前缀；如 `/plan`、`/order/{orderNo}/actions/{action}`。
- 状态值采用大写下划线风格（如 `CREATED`、`RUNNING`）。

### 1.7 文档结构

文档共六章：第 1 章"引言"概述目的、范围、术语；第 2 章"综合描述"给出产品定位与运行环境；第 3 章"外部接口需求"定义对外接口；第 4 章"系统特性"以特性维度详细描述功能；第 5 章"非功能需求"定义质量属性；第 6 章"附录"提供术语、参考与枚举说明。

---

## 2. 综合描述

### 2.1 产品定位

本系统是面向中小型制造企业的**轻量级 MES 后端服务**，对外以 RESTful API 形式提供能力。系统采用 Spring Boot 单体多模块架构，使用 MySQL 作为主数据存储，JWT 作为认证机制，RBAC 作为授权模型，并以"状态机 + 级联联动 + 审计追踪"作为核心业务骨架。

### 2.2 产品功能概述

系统的核心能力可归纳为三大领域：

| 领域 | 核心能力 |
|------|----------|
| **生产核心域** | 生产计划（Plan）、生产订单（Order）、工单（WorkOrder）的全生命周期管理；三层级联状态联动；状态变更审计 |
| **主数据域** | BOM、工艺流程、工序、工作步骤、设备、产线、班组（团队）、产线编组、技能矩阵的维护 |
| **支撑域** | RBAC 用户/角色/权限管理；JWT 登录与无状态鉴权；接口级权限校验；业务门禁（产能/工艺/技能） |

### 2.3 用户类与特性

系统按照数据库 `role` 表中的实际定义，划分为以下 6 类用户角色：

| 用户类 | 角色编码（`role_code`） | 角色描述 | 主要操作 |
|--------|------------------------|----------|----------|
| 生产主管 | `ROLE_PROD_MANAGER` | 负责生产计划的制定、发布和管理，监督生产进度，协调生产资源 | 创建/发布/取消发布/暂停/恢复/作废生产计划与生产订单；查看生产进度与审计记录 |
| 工艺工程师 | `ROLE_PROCESS_ENGINEER` | 负责工艺流程设计、BOM 管理、工序定义和工艺参数优化 | 维护 BOM、工艺流程、工序、工作步骤、设备等主数据；定义工艺参数与质量控制点 |
| 人事主管 | `ROLE_HR_MANAGER` | 负责人员管理、岗位分配、技能管理和权限分配 | 维护用户、角色、班组人员；分配用户-角色与角色-权限；维护员工技能矩阵 |
| 车间主任 | `ROLE_WORKSHOPS_DIRECTOR` | 负责车间整体管理、生产调度、设备管理和安全管理 | 暂停/恢复/作废生产订单与工单；监控产线状态；调度车间资源 |
| 班组长 | `ROLE_TEAM_LEADER` | 负责班组管理、工单分派、进度跟踪和质量控制 | 维护班组信息；分派/监督工单执行；跟踪生产进度与质量数据 |
| 工人 | `ROLE_WORKER` | 负责执行生产任务、报工和设备操作 | 接收工单；执行 START_WORK/FINISH_WORK；上报实际产量、合格品与不良品数量 |

> 角色编码、角色名称与描述均来源于数据库 `role` 表的真实数据；权限编码采用 `模块_功能` 命名格式（例如 `SYS_ROLE_MANAGE`、`SYS_USER_MANAGE`、`SYS_PERMISSION_ASSIGN` 等），由 `permission` 表统一维护，并通过 `role_permission` 表与角色绑定。

### 2.4 运行环境

| 项 | 要求 |
|----|------|
| 操作系统 | 主流 Linux / Windows Server / macOS（开发环境） |
| JDK | Java 21 及以上 |
| 应用服务器 | Spring Boot 3.5.7 内嵌 Tomcat |
| 数据库 | MySQL 8.x（支持 JSON 类型） |
| 构建工具 | Maven ≥ 3.9 |
| 客户端 | 任意支持 HTTP/HTTPS + JSON 的 Web 浏览器或移动端 |

### 2.5 设计与实现约束

- **单体多模块**：Maven 父子工程（pojo / common / production / server）部署为单个可执行 JAR。
- **包名规范**：所有代码位于 `com.xtax` 包下，按层级分包。
- **状态语义**：所有具有生命周期的业务对象使用统一的 `StateEnum`（CREATED/RELEASED/RUNNING/PAUSED/COMPLETED/TERMINATED）和 `ActionEnum`（PUBLISH/CANCEL_PUBLISH/PAUSE/RESUME/START_WORK/FINISH_WORK/TERMINATE）。
- **状态机纯净性**：状态机仅做规则校验，不访问数据库、不涉及权限。
- **无状态服务**：服务端不保存 Session，认证完全基于 JWT。
- **数据库一致性**：跨表写操作通过事务保证；级联联动通过 Service 层编排实现。
- **物理分页**：列表查询统一采用 PageHelper 物理分页，避免内存全量加载。
- **JSON 半结构化字段**：技能矩阵 `process_matrix` 等字段以 JSON 存储，通过 `JsonToMapTypeHandler` 转换。

### 2.6 假设与依赖

- 假设企业内部网络可靠，且服务器与数据库间网络延迟稳定。
- 假设 MySQL 已部署且可远程访问，且配置了合理的备份策略。
- 假设客户端在每次请求中正确携带 `Authorization: Bearer <token>` 或 `token` 头。
- 假设车间一线终端具备稳定网络连接以上报工单进度。

### 2.7 用户文档

随系统提供：

- API 接口文档（详见 `docx/三层生产架构/`、`docx/RBAC权限/`、`docx/静态信息维护/`）。
- 概要设计与详细设计文档（详见 `docx/概要设计说明书.md`、`docx/详细设计/`）。
- 数据库设计文档（详见 `docx/智联车间轻量化MES系统数据库设计.md`）。

---

## 3. 外部接口需求

### 3.1 用户接口（UI）

本系统为后端服务，**不直接提供 UI**，但需配合前端 Web/移动端使用。对前端的接口要求：

- 前端 SHALL 通过 HTTP/HTTPS 调用本系统 RESTful API。
- 前端 SHALL 在登录后将 JWT 持久化（推荐 LocalStorage 或安全 Cookie），并在后续请求中通过 `Authorization: Bearer <token>` 头携带。
- 前端 SHOULD 根据接口返回的 `code` 字段（1=成功，0=失败）做统一错误处理；HTTP 状态码 401/403/400/500 应分别映射为登录失效、权限不足、业务错误、服务器异常。
- 前端 SHOULD 支持中英文混排展示；状态值（如 `CREATED`/`RUNNING`）应进行本地化显示。

### 3.2 硬件接口

- 系统对硬件无直接控制需求；如需对接 PLC、扫码枪、RFID、看板大屏等设备，由前端或独立网关服务负责，本系统通过 RESTful API 接收上报数据（如开始工单、完成工单、上报数量）。
- 服务器硬件最低建议：2 核 CPU / 4 GB 内存 / 50 GB 磁盘。

### 3.3 软件接口

| 接口 | 类型 | 说明 |
|------|------|------|
| MySQL 8.x | 数据库 | JDBC 连接，承载所有持久化数据；支持 JSON 列类型 |
| MyBatis + PageHelper | ORM | 使用 XML 映射 + 注解；统一物理分页 |
| Spring Boot 3.5.7 | 框架 | 内嵌 Tomcat，单 JAR 部署 |
| jjwt 0.12.6 | JWT 库 | HS256 签名；24 小时有效期；密钥由应用配置管理 |
| Logback / SLF4J | 日志 | 控制台 + 滚动文件输出（详见非功能需求） |
| Lombok / Jackson | 工具库 | 实体注解简化、JSON 序列化 |

> 系统对外开放的可选集成点（未在当前版本实现）：ERP/WMS 数据交换接口、消息队列上报接口、报表系统数据导出接口。这些 MAY 在后续版本通过新增 Controller 提供。

### 3.4 通信接口

- **协议**：HTTP/1.1，生产环境 SHOULD 启用 HTTPS。
- **数据格式**：请求/响应均为 JSON（UTF-8 编码）；MIME 类型 `application/json`。
- **API 风格**：RESTful，兼具状态-动作模式（`POST /{resource}/{id}/actions/{action}`）。
- **认证方式**：JWT（HS256），通过 `Authorization: Bearer <token>` 头或自定义 `token` 头传递。
- **跨域**：开发/测试环境通过 `CorsConfig` 全部放通（允许所有来源、方法、请求头，预检缓存 3600 秒，支持凭证）；生产环境 SHOULD 收紧来源白名单。
- **统一响应**：
  - 单对象/操作：`{"code": 1|0, "message": "...", "data": {...}}`
  - 分页：`{"total": N, "rows": [...]}`
- **错误码**：HTTP 状态码 + 业务 `code` 双重表达；常见状态码 200/400/401/403/500。
- **分页参数**：`page`、`pageSize`，由 PageHelper 统一拦截。

---

## 4. 系统特性

> 本章按"特性 = 一组紧耦合的相关需求"组织。每个特性包含：4.x.1 描述与优先级、4.x.2 触发与响应、4.x.3 功能需求（编号化）。

### 4.1 特性：身份认证与登录（JWT）

#### 4.1.1 描述与优先级
**优先级：高**。系统所有功能性接口（除登录、错误页、白名单接口）必须通过 JWT 认证。

#### 4.1.2 触发与响应
- **触发**：客户端调用 `POST /login` 提交用户名/密码；或后续请求未携带有效 token。
- **响应**：登录成功签发 JWT；非法/过期 token 返回 HTTP 401。

#### 4.1.3 功能需求
- **REQ-AUTH-001**：系统 SHALL 提供 `POST /login` 接口，接受用户名与密码，校验通过后签发 JWT。
- **REQ-AUTH-002**：JWT SHALL 使用 HS256 算法签名，有效期 24 小时；Claims SHALL 包含至少 `id`（用户 ID，Integer）。
- **REQ-AUTH-003**：系统 SHALL 通过 `LoginFilter`（`@WebFilter "/*"`）对所有请求做 token 提取与校验，`/login` 路径放行。
- **REQ-AUTH-004**：token 解析失败、签名不正确、已过期 SHALL 返回 HTTP 401。
- **REQ-AUTH-005**：系统 SHALL 提供 `/auth` 下的接口供客户端获取/校验当前用户的权限编码集合。

### 4.2 特性：基于角色的访问控制（RBAC）

#### 4.2.1 描述与优先级
**优先级：高**。系统采用 5 表 RBAC 模型：`users` / `roles` / `permissions` / `user_role` / `role_permission`。

#### 4.2.2 触发与响应
- **触发**：用户访问受控接口；或管理员维护角色/权限/分配关系。
- **响应**：拥有权限放行；缺失权限返回 HTTP 403。

#### 4.2.3 功能需求
- **REQ-RBAC-001**：系统 SHALL 提供用户管理接口（`/user`）：增、删、改、查、按角色查询、按用户查询其角色等共约 6 个接口。
- **REQ-RBAC-002**：系统 SHALL 提供角色管理接口（`/role`）：CRUD 共约 5 个接口；写操作需要 `SYS_ROLE_MANAGE` 权限。
- **REQ-RBAC-003**：系统 SHALL 提供权限查询接口（`/permission`）：只读，约 3 个接口；GET 请求白名单放行，便于前端构建菜单。
- **REQ-RBAC-004**：系统 SHALL 提供用户-角色分配接口（`/user-role`）共约 4 个，需要相应权限。
- **REQ-RBAC-005**：系统 SHALL 提供角色-权限分配接口（`/role-permission`）共约 4 个，需要 `SYS_PERMISSION_ASSIGN` 权限。
- **REQ-RBAC-006**：权限编码 SHALL 采用 `模块_功能` 命名格式（如 `SYS_USER_MANAGE`）。
- **REQ-RBAC-007**：`PermissionInterceptor` SHALL 通过 `@RequirePermission` 注解判定接口权限：
  - 支持方法级与类级注解，方法级优先；
  - 支持 `Logical.AND`（全部满足）与 `Logical.OR`（任一满足）；
  - 校验失败返回 HTTP 403；
  - 排除路径：`/login`、`/error`、`/auth/**`；`GET /role/**`、`GET /permission/**` 白名单放行。

### 4.3 特性：生产计划（Plan）管理

#### 4.3.1 描述与优先级
**优先级：高**。生产计划是顶层生产任务。

#### 4.3.2 触发与响应
- **触发**：生产主管创建或调度计划。
- **响应**：CRUD 与状态动作（PUBLISH/CANCEL_PUBLISH/PAUSE/RESUME/TERMINATE）按需执行；状态变更联动子级订单。

#### 4.3.3 功能需求
- **REQ-PLAN-001**：系统 SHALL 提供 `/plan` 路径下的 CRUD 接口及分页查询；分页参数为 `page`、`pageSize`。
- **REQ-PLAN-002**：Plan 实体 SHALL 至少包含字段：`planNo`、`planName`、`bomId`、`planNum`、`completed`、`startTime`、`endTime`、`priority`、`status`、`creatorId`、`publisher` 等。
- **REQ-PLAN-003**：系统 SHALL 提供状态动作接口 `POST /plan/{planNo}/actions/{action}`，支持 PUBLISH、CANCEL_PUBLISH、PAUSE、RESUME、TERMINATE。
- **REQ-PLAN-004**：状态转换 SHALL 严格遵循 `planStateMachine` 的规则：
  - `CREATED → {PUBLISH}`
  - `RELEASED → {CANCEL_PUBLISH, TERMINATE}`
  - `RUNNING → {PAUSE}`
  - `PAUSED → {RESUME}`
  - `COMPLETED/TERMINATED → {}`（终态）
  非法转换 SHALL 返回 HTTP 400（`IllegalStateException`）。
- **REQ-PLAN-005**：仅生产主管角色可执行 PUBLISH/CANCEL_PUBLISH/PAUSE/RESUME/TERMINATE；权限不足 SHALL 返回 HTTP 403（`SecurityException`）。
- **REQ-PLAN-006**：PUBLISH 动作 SHALL 触发 GatePolicy 三级门禁（产能、工艺、技能），任一失败返回 HTTP 400（`BusinessException`）。
- **REQ-PLAN-007**：PUBLISH 后，系统 SHALL 根据 BOM 对应的工艺流程与可用产线，自动拆分生成多个 Order（CREATED 状态）。

### 4.4 特性：生产订单（Order）管理

#### 4.4.1 描述与优先级
**优先级：高**。生产订单是 Plan 到具体产线的分解。

#### 4.4.2 功能需求
- **REQ-ORD-001**：系统 SHALL 提供 `/order` 下约 9 个接口，覆盖 CRUD、状态动作与多维度查询（按 Plan、按产线、按状态等）。
- **REQ-ORD-002**：Order 实体 SHALL 至少包含：`orderNo`、`planNo`（FK）、`lineNo`（FK）、`orderName`、`quantity`、`quantityProduced`、`qualifiedProducts`、`defectiveProducts`、`status`、`startTime/endTime`、`actualStart/End` 等。
- **REQ-ORD-003**：状态动作支持 PUBLISH/CANCEL_PUBLISH/PAUSE/RESUME/START_WORK/FINISH_WORK/TERMINATE，遵循 `orderStateMachine`：
  - `CREATED → {PUBLISH}`
  - `RELEASED → {CANCEL_PUBLISH, START_WORK, TERMINATE}`
  - `RUNNING → {PAUSE, FINISH_WORK}`
  - `PAUSED → {RESUME}`
  - 终态：COMPLETED、TERMINATED。
- **REQ-ORD-004**：仅车间主任、生产主管可执行 PAUSE/RESUME/TERMINATE；START_WORK/FINISH_WORK 由具备相应权限的用户触发。
- **REQ-ORD-005**：PUBLISH Order 时，系统 SHALL 根据工艺流程的工序与技能矩阵，为每个工序-员工组合自动派生 WorkOrder，并自动进入 RELEASED 状态。
- **REQ-ORD-006**：START_WORK 动作 SHALL 锚定 `actualStartTime`；FINISH_WORK 动作 SHALL 锚定 `actualEndTime`。
- **REQ-ORD-007**：TERMINATE 前 SHALL 校验：不存在 RUNNING 子工单；否则返回 HTTP 400。

### 4.5 特性：工单（WorkOrder）管理

#### 4.5.1 描述与优先级
**优先级：高**。工单是最细粒度的执行单元。

#### 4.5.2 功能需求
- **REQ-WO-001**：系统 SHALL 提供 `/workOrder` 下约 9 个接口，覆盖 CRUD、状态动作及多维度查询（按 Order、按工序、按员工、按状态等）。
- **REQ-WO-002**：WorkOrder 实体 SHALL 至少包含：`workOrderNo`、`orderNo`（FK）、`processId`、`userId`、`isCritical`、`plannedQty`、`actualQty`、`scrapQty`、`status`、`startTime/endTime`、`actualStart/End` 等。
- **REQ-WO-003**：状态动作支持 PUBLISH/CANCEL_PUBLISH/PAUSE/RESUME/START_WORK/FINISH_WORK/TERMINATE，遵循 `workOrderStateMachine`：
  - `CREATED → {PUBLISH}`
  - `RELEASED → {CANCEL_PUBLISH, START_WORK, TERMINATE}`
  - `RUNNING → {PAUSE, FINISH_WORK}`
  - `PAUSED → {RESUME, TERMINATE}`
  - 终态：COMPLETED、TERMINATED。
- **REQ-WO-004**：员工角色可执行 START_WORK/FINISH_WORK；车间主任/生产主管可 TERMINATE。
- **REQ-WO-005**：START_WORK 前 SHALL 校验该工单的 `userId` 是否具备对应工序的技能（与技能矩阵一致）；不一致返回 HTTP 400。
- **REQ-WO-006**：仅 `isCritical = true` 的关键工单的状态变更 SHALL 触发向上聚合到 Order；非关键工单仅记录自身状态。

### 4.6 特性：三层级联状态联动

#### 4.6.1 描述与优先级
**优先级：高**。Plan → Order → WorkOrder 状态变更通过级联在层级间传播，是系统的核心架构特征。

#### 4.6.2 向下联动需求
- **REQ-CASC-D-001**：`Plan PUBLISH` SHALL 根据工艺流程和可用产线，拆分为多个 Order（CREATED 状态）。
- **REQ-CASC-D-002**：`Plan PAUSE` SHALL 级联暂停所有 RUNNING 状态的 Order，并进一步级联暂停其下 RUNNING 的 WorkOrder。
- **REQ-CASC-D-003**：`Plan RESUME` SHALL 级联恢复所有 PAUSED 状态的 Order 与其下 PAUSED 的 WorkOrder。
- **REQ-CASC-D-004**：`Plan CANCEL_PUBLISH` 或 `Plan TERMINATE` SHALL 级联作废所有非终态的 Order 与其下非终态的 WorkOrder。
- **REQ-CASC-D-005**：`Order PUBLISH` SHALL 为产线的各工序生成 WorkOrder 并自动 RELEASED。
- **REQ-CASC-D-006**：`Order PAUSE/RESUME/TERMINATE` SHALL 级联到所有关联 WorkOrder。

#### 4.6.3 向上同步需求
- **REQ-CASC-U-001**：WorkOrder 状态变更（仅 `is_critical = true`）SHALL 触发向上聚合 Order 的状态：
  - 任一关键工单 RUNNING → Order 置为 RUNNING；
  - 全部 COMPLETED → Order 置为 COMPLETED；
  - 任一 PAUSED → Order 置为 PAUSED。
- **REQ-CASC-U-002**：Order 状态变更 SHALL 触发 `syncPlanStatusFromOrders()` 单次遍历所有 Order（O(N)），按 RUNNING > COMPLETED > PAUSED 优先级聚合 Plan 状态。

#### 4.6.4 状态服务标准管道（8 步）
- **REQ-PIPE-001**：所有状态服务（`planStateServiceImpl` / `orderStateServiceImpl` / `workOrderStateServiceImpl`）的 `handle()` 方法 SHALL 遵循统一的 8 步流程：
  1. 加载业务对象，构建 `StateContext{bizNo, action, userId}`；
  2. 权限校验（`PermissionPolicy.check()`）；
  3. 状态机校验（`StateMachine.check()`）；
  4. 门禁校验（`GatePolicy.check()`，仅 PUBLISH 触发）；
  5. 业务校验（如 TERMINATE 前检查子对象、START_WORK 前检查员工技能）；
  6. 状态持久化（`fromStatus.next(action)` 计算新状态，按动作差异化锚定时间）；
  7. 写入审计（`AuditService` 写入 `status_history`）；
  8. 级联联动（`handleLinkage`）。

### 4.7 特性：业务门禁（产能 / 工艺 / 技能）

#### 4.7.1 描述与优先级
**优先级：高**。仅在 PUBLISH 动作时触发，保证生产任务可执行。

#### 4.7.2 功能需求
- **REQ-GATE-001**：**产能门禁（Capacity Gate）** SHALL 校验所选产线存在且状态为"空闲"。
- **REQ-GATE-002**：**工艺门禁（Process Gate）** SHALL 校验所选产线已绑定有效的工艺流程。
- **REQ-GATE-003**：**技能门禁（Skill Gate）** SHALL 校验工艺流程中的每个工序，均存在具备对应技能的员工。
- **REQ-GATE-004**：三级门禁 SHALL 按 Capacity → Process → Skill 顺序执行；任一失败立即拦截，抛出 `BusinessException`，HTTP 返回 400。

### 4.8 特性：BOM 物料清单管理

#### 4.8.1 功能需求
- **REQ-BOM-001**：系统 SHALL 提供 `/bom` 下约 6 个接口，支持 CRUD 与层级调整。
- **REQ-BOM-002**：BOM 实体 SHALL 至少包含：`id`、`parentId`、`drawingNo`、`nameSpec`、`material`、`quantity`、`unitWgt`、`type`。
- **REQ-BOM-003**：BOM 通过 `parentId` 自关联形成树结构，SHALL 支持任意深度。

### 4.9 特性：工艺流程与工序管理

#### 4.9.1 功能需求
- **REQ-FLOW-001**：系统 SHALL 提供 `/flow` 下约 5 个接口，支持工艺流程 CRUD 及流程图查询。
- **REQ-FLOW-002**：ProcessFlow 实体 SHALL 包含：`id`、`bomId`、`flowName`、`plannedWorkingHours`、`status`。
- **REQ-PROC-001**：系统 SHALL 提供 `/process` 下约 6 个接口，支持工序 CRUD。
- **REQ-PROC-002**：Processes 实体 SHALL 包含：`id`、`processName`、`plannedWorkingHours`、`qualityControlPoint`、`inputBomId(List)`、`outputBomId(List)`、`workStepId(List)`。

### 4.10 特性：工作步骤与设备管理

#### 4.10.1 功能需求
- **REQ-STEP-001**：系统 SHALL 提供 `/step` 下约 5 个接口，支持工作步骤 CRUD。
- **REQ-STEP-002**：WorkStep 实体 SHALL 包含：`id`、`name`、`equipmentId`、`functionId`、`equipmentName`、`equipmentModel`、`functionDescription`、`description`、`processName(List)`。
- **REQ-EQP-001**：系统 SHALL 提供 `/equipment` 下约 6 个接口，支持设备 CRUD。

### 4.11 特性：产线、班组与产线编组

#### 4.11.1 功能需求
- **REQ-LINE-001**：系统 SHALL 提供 `/line` 下约 6 个接口，支持产线 CRUD 与产线编组管理。
- **REQ-LINE-002**：ProductionLine 实体 SHALL 包含：`lineNo`、`lineName`、`lineStatus`、`flowId`、`flowName`。
- **REQ-LINE-003**：LineCompose 通过 `composes` 数组管理产线下的多个班组及技能矩阵；`process_matrix` 字段以 JSON 存储 `Map<工序, 技能等级>`，由 `JsonToMapTypeHandler` 转换。
- **REQ-TEAM-001**：系统 SHALL 提供 `/team` 下约 6 个接口，支持班组 CRUD 与技能矩阵维护。
- **REQ-TEAM-002**：WorkTeam 实体 SHALL 包含：`teamNo`、`teamName`、`teamLocation`、`teamLeader`、`memberNum`、`userName(List)`。

### 4.12 特性：技能矩阵与自动派工

#### 4.12.1 功能需求
- **REQ-SKILL-001**：技能矩阵表（`skills`）SHALL 存储字段：`username`、`process_name`、`choose`（技能等级）、`team_no`。
- **REQ-SKILL-002**：发布 Order 时 SHALL 调用 `findUserIdsBySkill()` 为每道工序查找具备对应技能的员工，并按工序-员工组合派发 WorkOrder。
- **REQ-SKILL-003**：每个 WorkOrder SHALL 设定一名员工为负责人（`userId`）。

### 4.13 特性：状态变更审计追踪

#### 4.13.1 功能需求
- **REQ-AUD-001**：所有 Plan/Order/WorkOrder 状态变更 SHALL 写入 `status_history` 表，字段包含：`id`、`targetType`（PLAN/ORDER/WORK_ORDER）、`targetNo`、`oldStatus`、`newStatus`、`operatorId`、`createdTime`。
- **REQ-AUD-002**：`AuditService` SHALL 提供 `getLastPauseRecord()` 方法，用于 RESUME 时获取上一次 PAUSE 的上下文。
- **REQ-AUD-003**：`operatorId` SHALL 从 JWT Claims 中的 `id` 字段提取，确保审计的不可伪造性。

### 4.14 特性：统一 RESTful API 规范

#### 4.14.1 功能需求
- **REQ-API-001**：系统 SHALL 遵循统一 URL 模式：
  - `GET /{resource}`：分页查询；
  - `GET /{resource}/{id}`：详情；
  - `POST /{resource}`：创建；
  - `PUT /{resource}/{id}`：更新；
  - `DELETE /{resource}/{id}`：删除；
  - `POST /{resource}/{id}/actions/{action}`：状态动作（专用于 Plan/Order/WorkOrder）。
- **REQ-API-002**：响应 SHALL 统一为 `Result` 或 `ResultPage` 结构。
- **REQ-API-003**：异常 SHALL 经 `globalExceptionHandler` 统一转换：
  - `BusinessException` → 400
  - `SecurityException` → 403
  - `ValidationException` → 400
  - `DuplicateKeyException` → 400
  - 其它 `Exception` → 500
- **REQ-API-004**：状态控制器 SHALL 额外处理 `IllegalStateException` → 400、`IllegalArgumentException` → 400。
- **REQ-API-005**：系统 SHALL 提供约 83 个 API 接口，覆盖认证授权、生产管理、主数据维护三大领域共计 17 个控制器（详见 [6.4 API 接口清单](#64-api-接口清单概要)）。

---

## 5. 非功能需求

### 5.1 性能需求

| 编号 | 要求 |
|------|------|
| **NFR-PERF-001** | 单次状态动作（含 8 步管道）在正常负载下 SHOULD 在 500 ms 内完成响应。 |
| **NFR-PERF-002** | 列表查询 SHALL 使用 PageHelper 物理分页，避免一次性加载全表；默认 `pageSize = 10`。 |
| **NFR-PERF-003** | 数据库连接池 SHOULD 合理配置（最大连接数 ≥ 20），高并发查询不应阻塞写入。 |
| **NFR-PERF-004** | Plan→Order 拆分、Order→WorkOrder 派工等批量操作 SHALL 在事务内一次性写入，避免 N+1 问题。 |
| **NFR-PERF-005** | 父级状态聚合 SHALL 采用 O(N) 单次遍历策略，避免重复查询。 |

### 5.2 安全需求

| 编号 | 要求 |
|------|------|
| **NFR-SEC-001** | 所有受控接口 SHALL 通过 JWT 认证 + RBAC 授权 + 业务策略 + 业务门禁 + 状态机的 5 层防线（详见概要设计 8.4 节）。 |
| **NFR-SEC-002** | JWT 密钥 MUST 通过应用配置管理；生产环境 MUST 使用足够强度的密钥（≥ 256 位）。 |
| **NFR-SEC-003** | JWT 有效期 SHALL 为 24 小时；过期后客户端 MUST 重新登录。 |
| **NFR-SEC-004** | 密码 SHALL 在数据库中以加盐哈希形式存储，禁止明文。 |
| **NFR-SEC-005** | 所有写操作 SHALL 记录 `operatorId`；状态变更 SHALL 写入 `status_history`。 |
| **NFR-SEC-006** | 生产环境 SHALL 启用 HTTPS，CORS 来源 SHOULD 收紧白名单（默认全放通仅适用于开发/测试）。 |
| **NFR-SEC-007** | 任何未捕获异常 SHALL 由 `globalExceptionHandler` 兜底返回 500，避免堆栈泄漏。 |

### 5.3 可靠性需求

| 编号 | 要求 |
|------|------|
| **NFR-REL-001** | 数据库写操作 SHALL 在事务边界内执行；级联操作失败 SHALL 整体回滚。 |
| **NFR-REL-002** | 状态机 SHALL 确保非法状态转换被拒绝；不依赖客户端校验。 |
| **NFR-REL-003** | 系统年可用性 SHOULD 达到 99%（按企业内部网络环境下评估）。 |
| **NFR-REL-004** | 关键业务对象（Plan/Order/WorkOrder）的删除 SHALL 受软删除/级联删除约束，避免数据孤岛。 |

### 5.4 可维护性需求

| 编号 | 要求 |
|------|------|
| **NFR-MAINT-001** | 代码 SHALL 遵循分层架构（Controller / Service / Domain / Infrastructure / POJO）；跨层调用受限。 |
| **NFR-MAINT-002** | Maven 模块依赖 SHALL 单向：server → production → common → pojo。 |
| **NFR-MAINT-003** | 状态机、权限策略、门禁策略 SHALL 通过接口隔离，允许独立替换。 |
| **NFR-MAINT-004** | SQL 日志 SHOULD 在开发环境输出，便于排查；生产环境 SHOULD 关闭以保护性能。 |
| **NFR-MAINT-005** | 关键流程（8 步管道）SHALL 在文档中保持与代码一致；任一变更需更新概要设计与 SRS。 |

### 5.5 可扩展性需求

| 编号 | 要求 |
|------|------|
| **NFR-EXT-001** | 新增状态或动作 SHALL 通过扩展 `StateEnum` / `ActionEnum` 与对应 `StateMachine` 注册即可，不修改 Service 主流程。 |
| **NFR-EXT-002** | 新增业务对象 SHALL 复用 8 步管道与 `StateContext` 模型，仅实现自身的 PermissionPolicy/GatePolicy。 |
| **NFR-EXT-003** | 新增权限编码 SHALL 仅需向 `permissions` 表插入记录并在 Controller 标注 `@RequirePermission`。 |
| **NFR-EXT-004** | 新增门禁规则 SHALL 通过实现 `GatePolicyInterface` 完成，按需嵌入责任链。 |

### 5.6 可移植性需求

| 编号 | 要求 |
|------|------|
| **NFR-PORT-001** | 系统 SHALL 打包为单 JAR，可在任意支持 Java 21 的环境运行。 |
| **NFR-PORT-002** | 数据库 SHALL 兼容 MySQL 8.x；非 MySQL 数据库需评估 JSON 类型与方言适配。 |
| **NFR-PORT-003** | 配置 SHALL 通过 `application.yml` 外部化，便于不同环境切换。 |

### 5.7 日志与合规需求

| 编号 | 要求 |
|------|------|
| **NFR-LOG-001** | 系统 SHALL 使用 Logback，输出到控制台 + 滚动文件 `/logs/smart-workshop.%d{...}.log`。 |
| **NFR-LOG-002** | 日志滚动策略：单文件 1 MB 或按天切分；最大保留 30 天。 |
| **NFR-LOG-003** | 默认日志级别 INFO；异常 SHALL 以 ERROR 级别记录完整堆栈。 |
| **NFR-LOG-004** | 状态变更必须有审计记录；审计数据 SHALL 长期保留，不随业务对象删除。 |

### 5.8 国际化与本地化

| 编号 | 要求 |
|------|------|
| **NFR-I18N-001** | 后端响应消息 MAY 提供中文；状态/动作枚举值固定为大写英文，便于跨端一致。 |
| **NFR-I18N-002** | 前端 SHOULD 自行实现状态值的中文显示映射。 |

---

## 6. 附录

### 6.1 术语表

| 术语 | 含义 |
|------|------|
| 生产计划（Plan） | 顶层生产任务，定义生产什么、多少、何时完成 |
| 生产订单（Order） | 分配给具体产线的生产指令，是 Plan 的分解 |
| 工单（WorkOrder） | 最细粒度的执行单元，分配给具体工序和员工 |
| BOM | 物料清单，定义产品的组成结构 |
| 工艺流程（ProcessFlow） | 产品生产的工序步骤和顺序 |
| 工序（Process） | 工艺流程中的一道操作步骤 |
| 工作步骤（WorkStep） | 工序执行所需的具体动作及关联的设备/功能 |
| 产线（ProductionLine） | 承载具体生产活动的物理或逻辑单元 |
| 产线编组（LineCompose） | 产线与班组的关联及技能矩阵 |
| 班组（WorkTeam） | 在产线上执行任务的人员小组 |
| 技能矩阵（Skill Matrix） | 员工-工序-技能等级的对应表 |
| 状态机 | 限定业务对象状态合法转换的规则集合 |
| 级联联动 | 父级状态变更引发子级变更，或子级状态聚合至父级的机制 |
| 业务门禁 | 在 PUBLISH 动作前对产能/工艺/技能的可执行性校验 |
| 审计追踪 | 完整记录状态变更历史，便于回溯与合规 |

### 6.2 缩略语

| 缩写 | 全称 | 含义 |
|------|------|------|
| MES | Manufacturing Execution System | 制造执行系统 |
| RBAC | Role-Based Access Control | 基于角色的访问控制 |
| JWT | JSON Web Token | 基于 JSON 的无状态令牌 |
| HS256 | HMAC SHA-256 | JWT 签名算法 |
| BOM | Bill of Materials | 物料清单 |
| API | Application Programming Interface | 应用程序接口 |
| REST | Representational State Transfer | 表述性状态转移 |
| ORM | Object-Relational Mapping | 对象关系映射 |
| CORS | Cross-Origin Resource Sharing | 跨域资源共享 |
| FK | Foreign Key | 外键 |
| TBD | To Be Determined | 待定 |

### 6.3 关键枚举说明

#### 6.3.1 StateEnum（生命周期状态）

```
CREATED ──▶ RELEASED ──▶ RUNNING ──┬──▶ COMPLETED
                 ▲                  │
                 │        ┌─────────┘
                 │        ▼
                 └──── PAUSED
                        ▲    │
                        └────┘ (RESUME)

TERMINATED（终态，仅可从 RELEASED/PAUSED 进入）
```

| 值 | 含义 |
|----|------|
| `CREATED` | 已创建，未发布 |
| `RELEASED` | 已发布，未开始 |
| `RUNNING` | 进行中 |
| `PAUSED` | 已暂停 |
| `COMPLETED` | 已完成（终态） |
| `TERMINATED` | 已作废/终止（终态） |

#### 6.3.2 ActionEnum（动作）

| 动作 | 含义 | 适用对象 |
|------|------|----------|
| `PUBLISH` | 发布 | Plan / Order / WorkOrder |
| `CANCEL_PUBLISH` | 取消发布 | Plan / Order / WorkOrder |
| `PAUSE` | 暂停 | Plan / Order / WorkOrder |
| `RESUME` | 恢复 | Plan / Order / WorkOrder |
| `START_WORK` | 开始生产 | Order / WorkOrder |
| `FINISH_WORK` | 完成生产 | Order / WorkOrder |
| `TERMINATE` | 作废/终止 | Plan / Order / WorkOrder |

### 6.4 API 接口清单（概要）

#### 6.4.1 认证与授权

| 控制器 | 路径前缀 | 接口数 | 说明 |
|--------|----------|--------|------|
| LoginController | `/login` | 1 | 登录获取 JWT |
| AuthController | `/auth` | 2 | 获取/校验当前用户权限 |
| UserController | `/user` | 6 | 用户 CRUD + 关联查询 |
| RoleController | `/role` | 5 | 角色 CRUD（写需要权限） |
| PermissionController | `/permission` | 3 | 权限查询（只读） |
| UserRoleController | `/user-role` | 4 | 用户-角色分配（需要权限） |
| RolePermissionController | `/role-permission` | 4 | 角色-权限分配（需要权限） |

#### 6.4.2 生产管理

| 控制器 | 路径前缀 | 接口数 | 说明 |
|--------|----------|--------|------|
| PlanController | `/plan` | 5 | 计划 CRUD + 状态动作 |
| OrderController | `/order` | 9 | 订单 CRUD + 状态动作 + 关联查询 |
| WorkOrderController | `/workOrder` | 9 | 工单 CRUD + 状态动作 + 多维度查询 |

#### 6.4.3 主数据管理

| 控制器 | 路径前缀 | 接口数 | 说明 |
|--------|----------|--------|------|
| BomController | `/bom` | 6 | BOM CRUD + 层级调整 |
| ProcessController | `/process` | 6 | 工序 CRUD |
| ProcessFlowController | `/flow` | 5 | 工艺流程 CRUD + 流程图 |
| EquipmentController | `/equipment` | 6 | 设备 CRUD |
| LineController | `/line` | 6 | 产线 CRUD + 编组管理 |
| TeamController | `/team` | 6 | 团队 CRUD + 技能矩阵 |
| WorkStepController | `/step` | 5 | 工作步骤 CRUD |

> 全系统约 17 个控制器、约 83 个 API。

### 6.5 数据库表清单（概要）

| 类别 | 表名 |
|------|------|
| 生产核心 | `plan`、`production_order`、`work_order` |
| 主数据 | `bom`、`processes`、`process_flow`、`work_step`、`equipment`、`function_description` |
| 产线团队 | `production_line`、`line_compose`、`work_team`、`skills` |
| RBAC | `users`、`roles`、`permissions`、`user_role`、`role_permission` |
| 审计 | `status_history` |

### 6.6 待定问题（TBD）

| 编号 | 待定问题 | 说明 |
|------|----------|------|
| TBD-001 | 与 ERP/WMS 的集成协议 | 当前版本未实现，后续需补充对接需求 |
| TBD-002 | 多租户支持 | 当前为单租户场景，后续如需 SaaS 化需重新设计 |
| TBD-003 | 高可用与水平扩展 | JWT 已支持无状态扩展，但数据库主从/分库分表方案待定 |
| TBD-004 | 生产环境 CORS 白名单 | 默认全放通，正式上线前需明确允许来源 |

### 6.7 修订历史

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|----------|
| v1.0 | 2026-06-12 | 系统逆推生成 | 基于 `docx/概要设计说明书.md` 等设计文档逆向推导，初版发布 |

---

> **文档编制**: 基于现有设计文档逆推 · **审核状态**: 待审核 · **最后更新**: 2026-06-12
